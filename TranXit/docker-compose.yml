version: '3.4'

networks:
  backend:
  

services:

  ocelotapigw:
    container_name: api-gateway
    image: ${DOCKER_REGISTRY-}ocelotapigw
    build:
      context: .
      dockerfile: OcelotApiGw/Dockerfile
    ports:
    - 61260:8080
    networks:
    - backend


  accountservice:
    container_name: account-service
    image: ${DOCKER_REGISTRY-}accountservice
    build:
      context: .
      dockerfile: AccountService/Dockerfile



  # emailmq:
  #   image: rabbitmq:3-management
  #   container_name: email-mq
  #   hostname: emailmq
  #   volumes:
  #       - ./.containers/queue/data/:/var/lib/rabbitmq
  #       - ./.containers/queue/log/:/var/log/rabbitmq
  #   ports:
  #     - 5672:5672
  #     - 15672:15672
  #   environment:
  #       RABBITMQ_DEFAULT_USER: tranxit
  #       RABBITMQ_DEFAULT_PASS: tranxit



  courierjobservice:
    image: ${DOCKER_REGISTRY-}courierjobservice
    build:
      context: .
      dockerfile: CourierJobService/Dockerfile

